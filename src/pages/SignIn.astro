---
import '../styles/global.css';
---
<html lang="es">
<meta charset="UTF-8">
<title>Iniciar Sesión - Nissan</title>
<style>
    .inputbox {
      position: relative;
      width: 100%;
      margin-bottom: 1.5rem;
    }

    .inputbox input {
      position: relative;
      width: 100%;
      padding: 20px 10px 10px;
      background: transparent;
      outline: none;
      box-shadow: none;
      border: none;
      color: #151515;
      font-size: 1em;
      letter-spacing: 0.05em;
      transition: 0.5s;
      z-index: 10;
      color: white;
    }

    .inputbox span {
        
      position: absolute;
      left: 0;
      padding: 20px 10px 10px;
      font-size: 1em;
      color: #4d4d4d;
      letter-spacing: 0.05em;
      transition: 0.5s;
      pointer-events: none;
    }

    .inputbox input:valid ~ span,
    .inputbox input:focus ~ span {
      color: #151515;
      transform: translateX(-10px) translateY(-34px);
      font-size: 0.75em;
    }

    .inputbox i {
      position: absolute;
      left: 0;
      bottom: 0;
      width: 100%;
      height: 2px;
      background: #666666;
      border-radius: 4px;
      transition: 0.5s;
      pointer-events: none;
      z-index: 9;
    }

    .inputbox input:valid ~ i,
    .inputbox input:focus ~ i {
      height: 44px;
    }
  </style>
<body class="bg-[#9C9C9C] flex items-center justify-center min-h-screen font-sans">

  <div class="bg-[#999999] p-10 rounded-2xl shadow-inner shadow-black  w-full max-w-md backdrop-blur-2xl z-0">
    <h1 class="text-3xl font-bold text-[#2E2E2E] mb-6 text-center">Iniciar Sesión</h1>

    <form action="/dashboard" method="POST">

      <div class="inputbox">
        <input type="email" name="email" required="required">
        <span>Correo electrónico</span>
        <i></i>
      </div>

      <div class="inputbox">
        <input type="password" name="password" required="required">
        <span>Contraseña</span>
        <i></i>
      </div>

      <button type="submit"
        class="w-full bg-[#666666] text-white font-semibold py-2 rounded-md hover:bg-[#2E2E2E] transition-colors">
        Iniciar sesión
      </button>

      <p class="text-center text-sm text-[#2E2E2E] mt-4">
        ¿No tienes cuenta? <a href="/Register" class="underline hover:text-[#000000]">Regístrate</a>
      </p>
    </form>
  </div>

</body>